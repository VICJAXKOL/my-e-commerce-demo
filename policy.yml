on_http_request:
  - expressions:
      - "!req.url.path.startsWith('/api/paystack/webhook')"
    actions:
      - type: basic-auth
        config:
          realm: "MyShop Preview"
          credentials:
            - "Victor:vicjaxkol26"
